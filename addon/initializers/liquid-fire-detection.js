export function initialize( container ) {
  // var lfDetection = container.lookup('service:liquid-fire-detection');
  // debugger;
  // console.log(!!container.lookupFactory('component:liquid-if'));
  // lfDetection.set('hasLiquidIf', !!container.lookupFactory('component:liquid-if'));
}

export default {
  name: 'liquid-fire-detection',
  initialize: initialize
};
